spring:
  application:
    name: service-consumer-other
  cloud:
    # nacos注册地址
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream # 监控服务需要访问这个接口，要忽略默认安全检测

feign:
  hystrix:
    enabled: true